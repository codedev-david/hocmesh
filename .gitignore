/target
*.db
*.db-shm
*.db-wal
.hocmesh/
node-*/
dist/
validator-*.db
.validator-*/
validators.json
*.profraw

# Staged by scripts/package-desktop.*: the node binary the desktop
# installer ships beside the window, named for the target triple.
crates/hocmesh-desktop/binaries/
# Written by tauri-build from capabilities/, regenerated every build.
crates/hocmesh-desktop/gen/

# Built or downloaded packages. `scripts/package-linux-*.sh` write into dist/,
# but a package fetched by hand to test the tooling lands here and must not be
# committed with the source.
*.deb
*.rpm
*.msi
*.pkg
*.AppImage
