name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version, for example v0.2.0"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            target: linux-x86_64
            archive: tar.gz
            features: mesh-gpu/cuda,mesh-gpu/rocm
          - os: windows-latest
            target: windows-x86_64
            archive: zip
            features: mesh-gpu/cuda
          - os: macos-latest
            target: macos-arm64
            archive: tar.gz
            features: mesh-gpu/metal
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.1
      - name: Build release binaries
        run: cargo build --release --workspace --features ${{ matrix.features }} --locked
      - name: Assemble release folder
        shell: pwsh
        run: |
          $dist = "mesh-${{ github.event.inputs.version || github.ref_name }}-${{ matrix.target }}"
          New-Item -ItemType Directory -Force $dist, "$dist/docs", "$dist/config" | Out-Null
          Copy-Item README.md, CODEX_HANDOFF.md, LICENSE, VERSION -Destination $dist
          Copy-Item docs/*.md -Destination "$dist/docs"
          Copy-Item config/*.json -Destination "$dist/config"
          if (Test-Path target/release/mesh.exe) {
            Copy-Item target/release/mesh.exe, target/release/mesh-coordinator.exe, target/release/mesh-validator.exe -Destination $dist
          } else {
            Copy-Item target/release/mesh, target/release/mesh-coordinator, target/release/mesh-validator -Destination $dist
          }
          "DIST=$dist" | Out-File -FilePath $env:GITHUB_ENV -Append
      - name: Create archive
        shell: pwsh
        run: |
          if ("${{ matrix.archive }}" -eq "zip") {
            Compress-Archive -Path "$env:DIST/*" -DestinationPath "$env:DIST.zip"
            "ARCHIVE=$env:DIST.zip" | Out-File -FilePath $env:GITHUB_ENV -Append
          } else {
            tar -czf "$env:DIST.tar.gz" "$env:DIST"
            "ARCHIVE=$env:DIST.tar.gz" | Out-File -FilePath $env:GITHUB_ENV -Append
          }
      - name: Build Linux installer
        if: runner.os == 'Linux'
        shell: bash
        run: |
          installer=$(./scripts/package-linux.sh target/release/mesh "${{ github.event.inputs.version || github.ref_name }}" dist amd64 | tail -1)
          echo "INSTALLER=$installer" >> "$GITHUB_ENV"
      - name: Build macOS installer
        if: runner.os == 'macOS'
        shell: bash
        run: |
          installer=$(./scripts/package-macos.sh target/release/mesh "${{ github.event.inputs.version || github.ref_name }}" dist | tail -1)
          echo "INSTALLER=$installer" >> "$GITHUB_ENV"
      - name: Install WiX
        if: runner.os == 'Windows'
        run: dotnet tool install --global wix --version 6.0.2
      - name: Build Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = "${{ github.event.inputs.version || github.ref_name }}"
          ./scripts/package-windows.ps1 -Binary target/release/mesh.exe -Version $version -OutputDirectory dist
          $installer = (Resolve-Path "dist/mesh-$($version.TrimStart('v'))-x86_64.msi").Path
          "INSTALLER=$installer" | Out-File -FilePath $env:GITHUB_ENV -Append
      - name: Generate checksums
        shell: pwsh
        run: |
          foreach ($artifact in @($env:ARCHIVE, $env:INSTALLER)) {
            $hash = Get-FileHash -Algorithm SHA256 $artifact
            "$($hash.Hash.ToLowerInvariant())  $(Split-Path -Leaf $artifact)" | Out-File -Encoding ascii "$artifact.sha256"
          }
      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: mesh-${{ matrix.target }}
          path: |
            ${{ env.ARCHIVE }}
            ${{ env.ARCHIVE }}.sha256
            ${{ env.INSTALLER }}
            ${{ env.INSTALLER }}.sha256
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v7
        with:
          path: artifacts
      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          name: MESH ${{ github.event.inputs.version || github.ref_name }}
          draft: true
          prerelease: true
          files: |
            artifacts/**/*.{zip,tar.gz,msi,pkg,deb,sha256}
